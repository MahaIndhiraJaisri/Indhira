#include<stdio.h>
void main()
{
char a[100];
int i,s;
length=strlen(a);
printf("Enter the string");
scanf("%s",&a);
for(i=o;i<=length;i++)
{
if(a[i]=='')
{
s++;
}
printf("The space of the string %d",s);
}
